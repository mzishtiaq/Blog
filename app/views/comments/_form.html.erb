<%= form_for([@article, @article.comments.build]) do |form| %>
  <p>
    <%= form.label :user_name %><br>
    <%= form.text_field :user_name %>
  </p>
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit "Submit" %>
  </p>
<% end %>
